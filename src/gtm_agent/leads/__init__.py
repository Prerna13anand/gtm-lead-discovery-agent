"""Part II — Lead Discovery & Matching (spec §§9-12, Phase 3).

Stage 6 (`leads.discovery`) · Stage 7 (`leads.matching`) · Stage 8
(`leads.enrichment`) · Stage 9 (`leads.company_context`), plus the
cross-cutting credit budget (`leads.budget`) and feedback capture
(`leads.feedback`) that Phase 3 also delivers (spec §22).
"""
